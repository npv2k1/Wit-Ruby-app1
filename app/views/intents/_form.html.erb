<%= form_with(model: intent, local: true) do |form| %>
  <% if intent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(intent.errors.count, "error") %> prohibited this intent from being saved:</h2>

      <ul>
        <% intent.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :intent %>
    <%= form.text_field :intent %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
